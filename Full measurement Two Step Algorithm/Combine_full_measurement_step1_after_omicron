def Combine_full_measurement_step1_after_omicron(Combine_full_measurement_step1):
    df = Combine_full_measurement_step1
    filtered_df = df.filter(col("condition_start_date") > "2021-11-20")

    return(filtered_df)
