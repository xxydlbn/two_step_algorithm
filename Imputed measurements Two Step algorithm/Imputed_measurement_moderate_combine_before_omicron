def Imputed_measurement_moderate_combine_before_omicron(moderate_step2_imp):
    df = moderate_step2_imp
    filtered_df = df.filter(col("condition_start_date") <= "2021-11-20")

    return(filtered_df)
