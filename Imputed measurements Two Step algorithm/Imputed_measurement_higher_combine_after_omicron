def Imputed_measurement_higher_combine_after_omicron(Imputed_measurement_higher_combine):
    df = Imputed_measurement_higher_combine
    filtered_df = df.filter(col("condition_start_date") > "2021-11-20")

    return(filtered_df)
