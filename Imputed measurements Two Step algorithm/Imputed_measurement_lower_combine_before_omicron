def Imputed_measurement_lower_combine_before_omicron(Imputed_measurement_lower_combine):
    df = Imputed_measurement_lower_combine
    filtered_df = df.filter(col("condition_start_date") <= "2021-11-20")

    return(filtered_df)
